#ifndef VARIABLES_H
#define VARIABLES_H

/* Variables-focused demos (no pointers here) */
void literals_and_types(void);              // integer/float/char literals, suffixes, ranges (printf)
void initialization_and_scope(void);        // default init, block scope vs file scope, shadowing
void format_specifiers_demo(void);          // printf/scanf specifiers for key types
void storage_duration_and_linkage(void);    // auto/static, internal/external linkage (overview)
void lvalues_rvalues_demo(void);            // assignment targets vs temporaries

#endif
